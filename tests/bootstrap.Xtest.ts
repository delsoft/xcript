// debugger;
// import { Encrypter } from "../index";

// describe('bootstrap',()=>{

//     test('should load xcripter package',()=>{
//      const obj = new Encrypter();
//      const e = obj.encrypt('a');
//      const d = obj.decrypt(e);

//      expect(d).toBe('a');
//      expect(e).not.toBe('a');
     
//     })
// })